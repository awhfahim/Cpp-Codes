#include <bits/stdc++.h>
#include <array>
using namespace std;

int main()
{
    /*
    int n;
    vector<int> nums;
    while(scanf("%d", &n)== 1)
    {
        printf("value %d\n", n);
        nums.push_back(n);
    }
    cout << "\n";
    printf("Input is Complete \n");
    for(auto it : nums){
        cout << it <<",";
    }
    */

    /*
    int arr[] = {7,2,5,9,210,345,213};
    sort(arr, arr + 7);
    for(auto e : arr)
    {
        cout << e << ",";
    }
    cout << endl ;

    string str;
    getline(cin, str);
    cout << str;
    */
    /*
    array<int,5> my = {2,5,7,5,8};
    cout << "My array Contains : ";
    for(auto it = my.begin(); it != my.end(); ++it){
        cout << ' ' << *it;
    }
    cout << "\n";
    string str;
    getline(cin, str);

    cout << "SIze :" << str.size() << endl;
    cout << str;
    */
    /*
    int t;
    cin >> t;
    getchar();
    while(t>0)
    {
        string name;
        getline(cin, name);
        cout << "Size:" << name.size()<< "\n";
        cout << "Content:" << name << "\n";
        t--;
    }
    */
    vector<vector<int>> vecofvec;
    vector<int> vec = {1,2,3};
    vecofvec.push_back(vec);
   std::ostream cout <<vecofvec<vector[0];


    return 0;
}
